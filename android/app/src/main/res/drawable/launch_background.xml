<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pure black background instead of white -->
    <item android:drawable="@android:color/black" />

    <!-- OPTIONAL: if you want *no* icon here, stop after the item above.
         If you want to keep your splash image centered, add a bitmap item like this:
    <item>
        <bitmap
            android:src="@mipmap/ic_launcher"
            android:gravity="center" />
    </item>
    -->

</layer-list>
