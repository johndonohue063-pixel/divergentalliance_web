import 'package:flutter/material.dart';
import '../security/pin_gate.dart';
import 'weather_center.dart';

class WeatherCenterGate extends StatelessWidget {
  static const route = '/weather';
  const WeatherCenterGate({super.key});

  @override
  Widget build(BuildContext context) {
    return PinGate(protectedChild: WeatherCenterScreen());
  }
}
