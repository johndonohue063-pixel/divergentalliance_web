import "package:flutter/material.dart";
import "../pages/weather_center.dart";
import "../ui/da_button.dart"; // expects class DAButton (see note below)
typedef DABtn = DAButton;
class CastleLanding extends StatelessWidget {
  const CastleLanding({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        fit: StackFit.expand,
        children: [
          Image.asset("assets/images/TRUCK_HERO.png", fit: BoxFit.cover),
          Container(color: Colors.black.withOpacity(0.60)),
          Align(
            alignment: Alignment.center,
            child: Padding(
              padding: const EdgeInsets.all(24),
              child: Column(
                mainAxisSize: MainAxisSize.min,
                children: [
                  const Text(
                    "Divergent Alliance",
                    style: TextStyle(
                      fontSize: 36,
                      fontWeight: FontWeight.bold,
                      color: Color(0xFFFF6A00),
                      letterSpacing: 1.5,
                    ),
                  ),
                  const SizedBox(height: 8),
                  const Text(
                    "Storm Response | Supply | Technology | Resiliency",
                    style: TextStyle(color: Colors.white70),
                    textAlign: TextAlign.center,
                  ),
                  const SizedBox(height: 6),
                  const Text(
                    "Request Services: Temporary Power | Fuel and Logistics | Debris Removal | Blue Tarping | Roofing | MRO Supply | IT and Network",
                    style: TextStyle(color: Colors.white60, fontSize: 12.5, height: 1.25),
                    textAlign: TextAlign.center,
                  ),
                  const SizedBox(height: 48),

                  // Weather Center, glossy orange
                  SizedBox(
                    width: 280,
                    child: DAButton(
                      label: "Weather Center",
                      icon: Icons.cloud,
                      onPressed: () => Navigator.pushNamed(context, WeatherCenter.route),
                      // primary variant is default
                    ),
                  ),

                  const SizedBox(height: 16),

                  // Store, dark variant
                  SizedBox(
                    width: 280,
                    child: DAButton(
                      label: "Store",
                      icon: Icons.storefront,
                      onPressed: () {},
                      variant: DAButtonVariant.secondary,
                    ),
                  ),
                ],
              ),
            ),
          ),
        ],
      ),
    );
  }
}
